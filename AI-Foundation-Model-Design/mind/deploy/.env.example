# Copy to .env and edit. NEVER commit .env.
DOMAIN=vio.example.com
# a long random access code — generate: python -c "import secrets;print(secrets.token_urlsafe(24))"
VIO_TOKEN=change-me-to-a-long-random-code
# optional: pin a model (blank = auto-pick whatever you pulled into Ollama)
VIO_LLM_MODEL=
